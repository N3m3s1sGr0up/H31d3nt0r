name: Secrets Scan
# PLACEHOLDER — the org's Argus secrets scanner was removed (2026-06-27).
# TODO: wire a replacement secrets scanner (e.g. gitleaks / trufflehog) here,
# then re-add its required status check to the org ruleset
# 'main-automated-baseline-gate' (see org-infra/variables.tf).
on:
  pull_request: { branches: [main] }
  push: { branches: [main] }
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  secrets-scan:
    runs-on: ubuntu-24.04
    steps:
      - name: Placeholder
        run: |
          echo "Secrets scanning is not configured: Argus was removed."
          echo "TODO: add a replacement scanner and restore the required check."
