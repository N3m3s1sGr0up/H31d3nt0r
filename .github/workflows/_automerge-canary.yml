name: _automerge-canary
# TEMPORARY (delete after validation): pins harden-runner to an old patch
# (v2.19.0 -> v2.19.4) so Dependabot proposes a patch bump, exercising the
# vendored auto-merge path end-to-end. The job never runs.
on:
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40 # v2.19.0
        with:
          egress-policy: audit
          disable-sudo-and-containers: true
