name: SOP Selftest
on:
  pull_request: { branches: [main] }
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  ping:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "inline workflow runs on H31d3nt0r"
